{
  "name": "delivery-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "3D Employee Delivery Game — stylized realistic driving game built with Three.js + Rapier3D",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
