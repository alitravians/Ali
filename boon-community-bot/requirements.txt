discord.py==2.4.0
aiohttp>=3.9,<4
python-dotenv>=1.0,<2
# Pillow is used by `bot.welcome_card` to render per-member welcome cards
# (avatar + arabic nickname + member number) on a cyber-green background.
# Requires system-level fontconfig + arabic fonts — see Dockerfile.
Pillow>=10.4,<12
# arabic-reshaper + python-bidi reorder arabic glyphs for correct rendering
# in Pillow (otherwise letters appear isolated / left-to-right).
arabic-reshaper>=3.0,<4
python-bidi>=0.4,<1
# sentry-sdk reports uncaught exceptions (welcome card render failures,
# discord gateway crashes, github webhook errors) to Sentry. The DSN is
# read at runtime from the SENTRY_DSN env var; if unset, the SDK is a
# no-op so local/CI runs stay identical to before.
sentry-sdk[aiohttp]>=2.18,<3
