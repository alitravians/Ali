# 📋 ملف التسليم للمطوّر الجديد — مدينة التبرعات (Donation City)

> هذا الملف هو **نقطة البداية** لأي مطوّر جديد (أو جلسة Devin جديدة) يستلم المشروع.
> يشرح: ما هو المشروع، أين الكود، القواعد الإلزامية، وين وصلنا في آخر جلسة، وكيف تكمّل باحترافية.
> اقرأه كاملاً قبل أي تعديل. للتفاصيل التقنية الموسّعة راجع `README.md` في جذر `donation-city/`.
>
> **⭐ أحدث جلسة (الأهم — اقرأها أولاً):** القسم **١٩** يلخّص كل أعمال آخر جلسة (سكربت النشر التلقائي `publish.py`، صندوق المكافآت، إكس-أو 3D، تحكّم الأدمن بوقت اليوم، إصلاح طيران الجوال، حاجز حدود الماب، طائرة الـspawn، إعادة تصميم الباركور، لافتة «أفضل وقت»). والقسم **٢٠** فيه **قواعد/سياسات جديدة إلزامية** ثبّتها صاحب المشروع في هذه الجلسة — التزم بها.
>
> **أعمال جلسات سابقة:**
> 1. **الباركور الجديد + الضابط + البوّابة:** اقرأ القسم **١٠** + القسم **١١**.
> 2. **القصر الجمهوري لشهد (داخل + خارج + بصمة + حرّاس):** اقرأ الأقسام **١٣ → ١٧**.
>
> وفي آخر الملف **القسم ١٨ «رسالة ترحيب للمطوّر الجديد»** — نصّ جاهز ترسله لأي مبرمج جديد مع الملف ليعرف من أين يبدأ بالضبط.

---

## 1) نظرة سريعة على المشروع

- **اللعبة:** مدينة سينما/تبرّعات تفاعلية على **Roblox** (واجهات عربية + دعم RTL).
- **Place ID:** `10237943037`
- **ملف اللعبة الرسمي:** `donation-city/DonationCity_FINAL.rbxlx` (يُفتح في Roblox Studio).
- **المصدر الحقيقي للكود:** مجلد `donation-city/src/` — **هنا تعدّل دائماً**، ثم تبني.
- **أداة البناء:** `donation-city/build_all.py` (تحقن سكربتات `src/` داخل ملف اللعبة).
- **المالك (Owner):** `Queen_Tarif` — UserId `2771986878` (ثابت في `CONFIG.AdminIds`).
- **المخزن (Repo):** `github.com/alitravians/Ali` — كل المشروع داخل مجلد `donation-city/`.

المشروع يشمل: نظام كوينز/تذاكر، مقاعد سينما + عروض، متجر بالـ Robux (Developer Products + Game Passes)، رتب وإدارة، وضع صيانة دائم، دردشة مخصّصة + **نظام أوامر**، نافورة، حوض سمك، **باركور احترافي كامل** (مسار أسطح مدينة ١٤ منصّة + ضابط شرطة بحريني + بوّابة دخول 3D)، **القصر الجمهوري لشهد** (داخل مؤثَّث بأربع غرف + قشرة خارجية فخمة + قفل بصمة للأدمن + سجلّ دخول + حرّاس يرحّبون ويرافقون)، ساحة ألعاب، شاطئ، أكشاك (Booths)، وشباك تذاكر.

---

## 2) ⚠️ القواعد الإلزامية (لا تتجاوزها أبداً)

هذه قواعد فرضها صاحب المشروع عبر الجلسات، والالتزام بها شرط أساسي:

1. **عدّل في `src/` فقط — لا تعدّل داخل ملف الـ `.rbxlx` يدوياً.** بعد التعديل شغّل `build_all.py`.
2. **لا تنسخ أنظمة إدارة جاهزة** (Adonis / HD Admin / Kohl's). أي ميزة إدارية تُبنى داخل أنظمتنا نفسها بلمسة احترافية أصلية.
3. **أمان الموديلات الخام (`*.rbxmx` / `*.rbxm`):** موديلات Creator Store غالباً فيها **باك-دور** (عائلة `LightConfig`/`Type`/`Layout`/`EasyConfiguration`/`Structure`/`Pose` التي تستعمل `require(assetId)` عبر قيمة `NumberPose` متنكّرة = تنفيذ كود خارجي). القاعدة:
   - **ديكور ثابت** (مثل الحوض): جرّد **كل** السكربتات بالكامل (`inject_aquarium.py` يفعل ذلك عبر مجموعة `STRIP`).
   - **موديل له سلوك مشروع** (مثل شباك التذاكر بأبوابه/أصواته): احذف **فقط** الباك-دور المؤكّد وأبقِ السكربتات النظيفة كما هي، ثم افحص المتبقّي من `require(`/`loadstring`/`getfenv`/`HttpGet` (`inject_ticketbooth.py`).
   - **لا يصل أي كود خبيث للسيرفر إطلاقاً.** كل BasePart يُثبّت `Anchored=true`.
4. **عند مشاركة المستخدم صورة مرجعية لتصميم:** استخرج الفكرة ولا تنسخها ١:١ — أضف لمسة المطوّر وأخرِج شيئاً **أفضل** من المرجع.
5. **الإصلاح يكون جذرياً وكاملاً** (طبقات حماية + احتياطات)، لا حلول مؤقّتة. اللاعب يجب ألا يرى حالة معطوبة أبداً.
6. **لا تختبر داخل Studio بنفسك** — صاحب المشروع يختبر بعد التسليم. سلّم ملفاً نظيفاً + شرح كيف يجرّب.
7. **بعد كل تعديل:** افحص صياغة Lua (luau)، ابنِ، وتأكد أن XML سليم (`well-formed`) وعدد كتل CDATA كامل، وصفر باك-دور.

---

## 3) بنية المشروع وأين كل شيء

```
donation-city/
├── DonationCity_FINAL.rbxlx     ← ملف اللعبة الكامل (المصدر الرسمي للنشر)
├── build_all.py                 ← أداة البناء: تحقن سكربتات src/ داخل ملف اللعبة
├── src/                         ← المصدر الحقيقي لكل السكربتات (عدّل هنا)
├── inject_*.py                  ← حواقن الموديلات (حقن هندسة + تجريد باك-دور). لمرّة واحدة/قابلة للتكرار
├── inject_cinema_new.py         ← المصدر الوحيد لهندسة السينما الحديثة (Marquee/InfoBoard/الشاشة/الكشك…)
├── render_cinema.py             ← أداة رندر سريعة: ترسم واجهة السينما من الـ.rbxlx للتحقق البصري بلا Studio
├── *.rbxmx / *.rbxm             ← موديلات Creator Store الخام (محفوظة ليكون الحقن قابلاً للتكرار)
├── icons/                       ← أيقونات المتجر
└── docs/                        ← هذا الملف + دراسة متجر المبدعين + تعليمات السلايد شو
```

### أهم سكربتات `src/` ومكانها في Studio

| الملف | المكان في Studio | الوظيفة |
|------|------------------|---------|
| `CinemaSystem.server.lua` | ServerScriptService | منطق السينما: العروض، المقاعد، صف الانتظار، الدفع عند المقعد + **صوت العرض** |
| `CinemaServices.server.lua` | ServerScriptService | الكوينز، الرتب، **المتجر** (Dev Products + Game Passes)، **لوحة الإدارة (backend)** + **تبويب صلاحيات الأوامر** |
| `CinemaClient.client.lua` | StarterPlayerScripts | كل واجهات اللاعب + **لوحة الإدارة (UI)** بما فيها تبويب «⌘ الأوامر» |
| `CustomChat.server.lua` | ServerScriptService | الدردشة المخصّصة + **نظام أوامر الدردشة** (معالج/صلاحيات/DataStore/تجميد/كتم) |
| `CustomChat.client.lua` | StarterPlayerScripts | واجهة الدردشة + **لوحة الأوامر** + **متحكّم الطيران** (client-side) |
| `TicketSystem.server.lua` | ServerScriptService | اقتصاد التذاكر + مكافأة يومية |
| `CinemaDecor.server.lua` | ServerScriptService | الديكور والإضاءة + **حركة السمك داخل حدود الحوض المخبوزة** |
| `ParkourSystem.server.lua` | ServerScriptService | **نظام الباركور الكامل** (هندسة + ميكانيكا + الضابط + بوّابة الدخول) — كله قطع أصلية في هذا الملف. راجع القسم ١٠ |
| `ParkourClient.client.lua` | StarterPlayerScripts | واجهة الباركور: شريط الحالة (المؤقّت · النقطة · النسبة · زر إيقاف) |
| `PalaceSystem.server.lua` | ServerScriptService | **القصر الجمهوري كامل** (قطع أصلية وقت التشغيل): القشرة الخارجية + الغرف الأربع المؤثَّثة + أبواب E + لافتات + **قفل البصمة + سجلّ الدخول (DataStore)**. راجع الأقسام ١٣–١٥ |
| `PalaceClient.client.lua` | StarterPlayerScripts | واجهات اللاعب للقصر: شاشة مسح البصمة (حلقات بصمة مرسومة أصلية + خطّ مسح) + عارض سجلّ الدخول للأدمن. راجع القسم ١٥ |
| `GuardSystem.server.lua` | ServerScriptService | حرّاس القصر الجمهوري (R6 بأنميشن + Pathfinding + ترحيب + مرافقة) — **مستقل عن ضابط الباركور**. راجع القسم ١٦ |
| `BoothSystem.server.lua` | ServerScriptService | الأكشاك والتفاعل بزر E |
| `WorldBuilder.server.lua` | — (مرجعي فقط) | بنى الخريطة برمجياً ثم خُبزت كمجسّمات ثابتة. **غير مُدرَج في `build_all.py`** |

> **استثناء مهم:** `WorldBuilder.server.lua` نتيجته مخبوزة في ملف اللعبة، فتعديله **لا ينعكس**. للمرجع فقط.

---

## ⚠️⚠️ أهم درس (اقرأه مرّتين): الهندسة الثابتة مقابل هندسة وقت التشغيل (Runtime)

> **سبب تكرار أخطاء «أصلحته بس ما تغيّر باللعبة» هو الخلط بين نوعَي الهندسة. افهم الفرق جيداً قبل أي تعديل على السينما.**

في هذا المشروع توجد العناصر بطريقتين مختلفتين تماماً:

1. **هندسة ثابتة (Static):** أجزاء (`Part`) مكتوبة مباشرة داخل `DonationCity_FINAL.rbxlx` بإحداثيات `CFrame` وحجم `size`. مصدرها `inject_cinema_new.py`. تظهر في فحص الملف XML.
2. **هندسة وقت التشغيل (Runtime):** عناصر **يُنشئها سكربت Lua لحظة تشغيل اللعبة** (`Instance.new` داخل `CinemaSystem`/`CinemaServices`). **لا تظهر إطلاقاً في فحص XML للملف الثابت** — تظهر فقط داخل اللعبة المشغّلة.

**القاعدة الحاسمة:**
- لو طلب المستخدم حذف/تعديل عنصر وأنت ما لقيته في الـXML ⇒ هو **يُبنى وقت التشغيل**. لاحق السكربت اللي ينشئه (ابحث عن اسمه أو نصّه داخل `src/*.server.lua`) وعدّله **هناك**، ثم ابنِ.
- بعد أي تعديل على نص/عنصر runtime، **تحقّق أنه فعلاً مُضمَّن داخل الكتلة `<![CDATA[ ]]>` في الملف النهائي** (مو بس في `src/`). ابحث عن النص داخل `DonationCity_FINAL.rbxlx`.
- أمثلة عناصر runtime في السينما: نص الماركي، محتوى لوحة التعليمات (SurfaceGui)، وكان منها «لاونج VIP» الأصفر (حُذف).

**مثال تحقّق سريع:**
```bash
# هل اختفى نص/عنصر runtime من الملف النهائي فعلاً؟
grep -c "النص_أو_اسم_العنصر" DonationCity_FINAL.rbxlx   # 0 = محذوف/غير موجود
# تحقّق بصري لواجهة السينما بلا Studio:
python3 render_cinema.py    # ينتج cinema_render.png
```

---

## 4) طريقة البناء والنشر (احترافياً)

```bash
cd donation-city
python3 build_all.py        # يحقن كل src/ داخل DonationCity_FINAL.rbxlx
```
- `build_all.py` يبحث عن **علامة فريدة (marker)** داخل كل كتلة `<![CDATA[ ... ]]>` ويستبدل محتواها بمصدر `src/`. الربط في متغير `UPDATES` أعلى الملف.
- **تحقّق بعد البناء (إلزامي):**
  ```bash
  python3 -c "import xml.etree.ElementTree as ET; ET.parse('DonationCity_FINAL.rbxlx'); print('XML OK')"
  ```
  وتأكد أن عدد كتل CDATA كامل وصفر باك-دور.
- **فحص صياغة Lua قبل البناء** (موصى به): يوجد ثنائي `luau`/`luau-analyze` في بيئة التطوير لفحص الأخطاء.

**النشر اليدوي (الطريقة القديمة):** افتح `DonationCity_FINAL.rbxlx` في Studio → **Publish to Roblox** على Place `10237943037`. جرّب من **اللعبة المنشورة** (مو زر Play في الاستديو) لمزايا DataStore/Robux، مع تفعيل **Enable Studio Access to API Services** من Creator Dashboard.

### ⚡ النشر التلقائي `publish.py` (الطريقة المعتمدة الآن — أُنشئت هذه الجلسة)
بدل فتح Studio يدوياً، يوجد سكربت ينشر مباشرة على روبلوكس عبر **Open Cloud API**:
```bash
cd donation-city
python3 publish.py               # فحص (selene/luau) + بناء (build_all.py) + رفع على Place 10237943037
python3 publish.py --skip-lint   # نفس الشي بدون مرحلة الفحص (لو فحصت يدوياً قبلها)
```
- **خط الأنابيب:** يفحص الصياغة → يشغّل `build_all.py` → يرفع `DonationCity_FINAL.rbxlx` عبر Open Cloud → يطبع **رقم النسخة** (Version) الجديد عند النجاح.
- **المفتاح السرّي:** يقرأ `ROBLOX_API_KEY` (Open Cloud API Key بصلاحية `universe-places:write` على Place `10237943037`) من البيئة/السرّيات. لو غير موجود، أضِفه قبل التشغيل.
- **التفاصيل الكاملة:** راجع `donation-city/PUBLISH_GUIDE.md` (دليل شامل: كيف تُنشئ مفتاح Open Cloud، الصلاحيات، استكشاف الأخطاء).
- **مهم:** بعد أي نشر، رقم النسخة (vNNN) اللي يطبعه السكربت هو رقم روبلوكس الداخلي — وهو **مختلف** عن «رقم الإصدار» المعروض للاعبين في `LoadingScreen.client.lua` (راجع القاعدة في القسم ٢٠).

---

## 5) ⌘ نظام أوامر الدردشة (ميزة قائمة — مرجع)

أُضيف نظام أوامر كامل **قابل للتحكم** من لوحة الإدارة، مبني داخل دردشتنا (بدون أي نظام خارجي).

### الأوامر والمستويات الافتراضية
- 🟢 **للجميع:** `/help` `/clear` `/me` `/roll` `/flip` `/coins` `/tickets` `/online` `/spawn` `/vip`
- 🔰 **للمشرفين (mod فأعلى):** `/announce` `/mute` `/unmute`
- 🛡️ **للأداريين (admin فأعلى):** `/kick` `/pull`(=`/bring`) `/to` `/freeze` `/unfreeze` `/fly` `/give` `/speed` `/heal`

كل أمر يدعم **مرادفات عربية/إنجليزية** (مثل `/طيران` = `/fly`، `/سحب` = `/pull`).

### آلية العمل (مهم للفهم)
- أي رسالة تبدأ بـ `/` → تُلتقط في `sayRemote.OnServerEvent` (في `CustomChat.server.lua`) وتُمرّر إلى `handleCommand` **ولا تُبَثّ كدردشة**.
- `CMD_ALIAS` يحوّل المرادف إلى **مفتاح قانوني (key)**، ثم **بوابة صلاحية موحّدة** تتحقق من `cmdLevel[key]` (المخزّن في DataStore).
- المستويات: `0=الجميع · 2=المشرفون · 3=الأداريون · 99=معطّل`. تُحفظ في DataStore باسم `ChatCmdPerms_v1`، وتُبثّ فوراً لكل العملاء عند أي تغيير.
- **لوحة الإدارة:** تبويب «⌘ الأوامر» (في `CinemaClient.client.lua`) يعرض كل أمر مع ٤ أزرار لاختيار مستواه. الـ backend في `CinemaServices.server.lua` عبر إجراء `setCmdPerm`.
- **لوحة الأوامر للاعب:** زر ⌘ في رأس الدردشة أو أمر `/help` — تبيّن المتاح لك مقابل المقفول.
- **الطيران `/fly`:** زر تبديل (toggle) — نفس الأمر يشغّل ويوقف. WASD للحركة، Space للأعلى، Ctrl للأسفل، Shift للسرعة. `/fly 120` لتحديد السرعة (16–300). يتوقف تلقائياً عند الموت/إعادة الظهور.
- **التجميد `/freeze`:** يُخزَّن في الذاكرة (`frozen[userId]`) ويُعاد تطبيقه عند كل ظهور للشخصية.

### نقاط أمان مطبّقة (من مراجعة Devin)
- `/spawn` فيه **مهلة ٥ ثوانٍ لكل لاعب** تمنع تكرار `LoadCharacter` (إجهاد السيرفر).
- **المكتوم** يُمنع فقط من أوامر **البثّ** (`/me` `/roll` `/flip` `/announce`) عبر مجموعة `BROADCAST_CMD`، لكنه يستطيع استخدام الأوامر الذاتية (`/help` `/clear` `/spawn` …).
- **سبب `/kick`** يمرّ على فلتر روبلوكس (`safeFilter`) قبل عرضه للاعب المطرود.
- لا يمكن كتم/طرد/تجميد من رتبته مثلك أو أعلى (الفحص عبر وزن الرتبة).

---

## 6) 🗺️ جلسة سابقة — السينما الحديثة (مبنى + تنظيف الواجهة)

> أحدث جلسة فيها عملان: **الباركور** (القسم ١١) و**القصر الجمهوري** (الأقسام ١٣–١٧). هذا القسم يوثّق جلسة السينما السابقة لهما.

استُبدل مبنى السينما القديم بموديل **«Cinema Modern»** (Asset `8841720651`) ثم جرت سلسلة إصلاحات على اتجاهه وواجهته. العمل على فرع `devin/1780716125-cinema-fixes` (**PR #238**) مع مقدّمة في **PR #237** (استبدال المبنى).

| العنصر | ماذا تم | النوع |
|--------|---------|-------|
| 🏛️ **مبنى السينما** | استُبدل بموديل حديث (`8841720651`). أُصلح خطأ فتح Studio «Unknown referenced shared string» عبر **دمج تعريفات SharedStrings الناقصة تلقائياً** في `inject_cinema_new.py` (تحقق: ٠ مرجع يتيم). | ثابت |
| 🔄 **اتجاه المدخل** | دُوّر المبنى −٩٠° فصارت الواجهة الزجاجية تواجه الساحة/اللاعب (المدخل أمام، الشاشة خلف، المقاعد وسط). | ثابت |
| 🚪 **الباب** | أبواب الواجهة + الأبواب الداخلية صارت قابلة للمرور؛ الزجاج الجانبي/الخلفي صلب؛ البوابة تقفل أثناء العرض. | ثابت/Runtime |
| 🪑 **اتجاه المقاعد** | عُدّلت لتواجه الشاشة (كانت معكوسة). | ثابت |
| 🟡 **لاونج VIP الأصفر** | كان يُبنى **وقت التشغيل** في `CinemaServices` (سجادة + أعمدة + مقاعد ذهبية Neon) — إضاءته القوية تخرّب المكان. **حُذف بالكامل** (٠ نيون أصفر). | **Runtime** |
| 🔴 **الماركي (اسم السينما)** | كان صندوقاً أحمر **طايفاً ٤ ستد قدام الواجهة**. ثُبّت **ملاصقاً للواجهة** (وسط المبنى، Z≈−100.5) وصار **شبه شفاف (Tr=0.85)** فلا يبان صندوق — يبان فقط نص «سينما مدينة التبرعات» (أبيض) داخل لوحة داكنة بهوامش وحدود مدوّرة. النص نفسه يُكتب **وقت التشغيل** في `CinemaSystem`. | ثابت + Runtime |
| 📋 **لوحة التعليمات (InfoBoard)** | كانت واقفة بالممر وتسدّ الدخول. ثُبّتت **ملاصقة لجدار الواجهة** يسار المدخل بمستوى النظر (Tr=0.8). محتواها SurfaceGui وقت التشغيل. | ثابت + Runtime |
| ⭐ **اتساق مزايا VIP** | بعد حذف اللاونج (الذي كان آلية الوجبة الحصرية الوحيدة)، أُزيل من كل مكان ذكر «لاونج خاص» **و«وجبة مجانية»** (الأخيرة لم تعد حصرية لـVIP لأن الخادم/البسطة يقدّمان الأكل للجميع). شمل ذلك: إشعار التفعيل + وصف المتجر + لوحة المرشد + شاشة التحميل. المزايا الآن الدقيقة: تذاكر بنص السعر · دخل مضاعف · أولوية بالصف · تاج ذهبي. | نصوص |
| 🟨 **لافتة TICKETS الصفراء** | حُذفت من فوق كشك التذاكر. | ثابت |

> ⚠️ **ملاحظة صلاحيات روبلوكس (خارج الكود):** عند النشر يطلب روبلوكس **Grant Permissions** لأي Asset مملوك لحساب آخر (مثل حركات/Animations الـNPCs) — هذا طبيعي ١٠٠٪، اضغط Grant All. القائمة تراكمية (ما مُنح سابقاً لا يظهر ثانية)، فنقصان العدد ليس حذفاً.

### إحداثيات الواجهة (مرجع سريع لأي تعديل مستقبلي على السينما)
- بصمة المبنى بعد التدوير: `X ∈ [−27.7, +27.7]` · `Z ∈ [−170.4, −91.2]` · `Y ∈ [0.5, 27.7]`.
- مستوى الواجهة الأمامية (حيث يُثبَّت أي عنصر سطحي): `Z ≈ −100.5` تقريباً.
- يدخل اللاعب من جهة `Z` الأكبر (الساحة) نحو `−Z` (المدخل).
- **عند إضافة أي لوحة/لافتة سطحية:** ثبّتها على `Z ≈ −100.5` (ملاصقة) ولا تتركها طايفة للأمام، واجعل الجزء الحامل شبه شفاف ليبان محتوى الـSurfaceGui فقط بلا صندوق.

### حالة الدمج (Merge)
- `#237` (استبدال المبنى) ثم `#238` (إصلاحات الاتجاه/الباب/الواجهة) — كلها فوق فرع المشروع `arabic-localization`.
- CI **أخضر ٦/٦** على #238، وكل ملاحظات Devin Review معالَجة (آخرها: إزالة سطر ميت في أداة الرندر + إزالة وعد «لاونج خاص» من مزايا VIP).

---

## 7) 🔞 تصنيف نضج المحتوى (الاستبيان) — مهم جداً للتشغيل

هذا أهم بند **تشغيلي** (خارج الكود) ظهر بعد جلسة السينما، وهو سبب «مشكلة الماب» التي ظنّها المالك خللاً برمجياً.

- **رابط الاستبيان:** https://create.roblox.com/dashboard/creations/experiences/10237943037/experience-questionnaire
- **القاعدة الحاسمة:** إذا كان تصنيف التجربة `Unknown` (استبيان غير مكتمل/مرفوض) فإن روبلوكس **لا يعرضها ولا يسمح بلعبها** → تبدو «الماب مختفية/غير متاحة». فلا بد من **استبيان مقبول** دائماً.
- **سياسة Social Hangout:** أي تجربة جوهرها «التجمّع/الدردشة/الأنشطة المشتركة» تُصنَّف `Social Hangout` = إجبارياً **13+ (Moderate)**. **لا يمكن** الجمع بين «Social Hangout = Present» و«كل الأعمار» إطلاقاً. ولو وصف/عنوان اللعبة يلمّح لتجمّع اجتماعي تُصنَّف تلقائياً Social Hangout.

### الحل المطبّق (Path 1 — إعادة التأطير) — PR #240
لتحقيق **كل الأعمار** بصدق، أعيد تأطير هوية اللعبة كـ«مهام/تحديات/باركور» بدل «تجمّع/سينما تشات»، دون حذف أي نظام:
- شاشة التحميل + الترحيب + دليل المبتدئين تبدأ بالمهام/الباركور/الأنشطة.
- إبراز زر «📋 المهام» (نبضة جذب) ولوحة «أفضل وقت/رقم قياسي» للباركور.
- السينما + الدردشة + التبرّع تبقى **ميزات جانبية** شغّالة كما هي.
- تحديث **العنوان/الوصف** في لوحة Roblox نحو النشاط المنظّم.

بعدها صار جواب **Social Hangout = No** صادقاً، فأعطى الاستبيان:
**Content Maturity = Minimal (كل الأعمار) · Descriptors: None · Non-Compliant Regions: None · Age Restriction: None.**

### ⚠ تحذيرات إلزامية
- **لا تكذب في الإجابات** — التحريف المتعمّد للمحتوى يعرّض الحساب لعقوبات من روبلوكس. الإجابات الصادقة بعد إعادة التأطير هي ما أعطت أدنى تصنيف.
- **المُراجِع البشري قد يعيد المراجعة** خلال أيام ويغيّر التصنيف (سبق أن تجاهل جواب «Not Present» وقرّر «Present» بنفسه). راقب إشعارات Roblox؛ لو رجع رفض/تغيّر التصنيف → قدّم **استئناف (Appeal)** بأدلّة (تعديلات #240 + الوصف الجديد).
- **يجب نشر ملف اللعبة المعاد تأطيره** (من #240 فأحدث) ليبقى التصنيف الصادق متوافقاً مع ما يراه اللاعب فعلاً.

---

## 8) 🎬 آخر الإصلاحات البصرية للسينما (بعد #238)

| العنصر | ماذا تم | PR |
|--------|---------|----|
| 🔴 **خط النيون الأحمر أمام الشاشة** | كان شريطا النيون الجانبيان على ارتفاع `topY + 3` (مستوى الشاشة/العين)، فمن زاوية المقاعد يظهران كخط أحمر يعبر أمام الشاشة وتوهّجه (Bloom) يطمس لوحة شباك التذاكر المجاورة. **الحل الجذري:** إنزالهما لقاعدة الجدارين `topY + 0.2` (أسفل خط النظر) + تنحيف (`0.3×0.18`) + تخفيف الإضاءة (`0.22/range7`) + تقصير الطول بعيداً عن جدار الشاشة. الملف: `src/CinemaSystem.server.lua` (~٥٧٨–٥٨٦). | #241 |

### 🧱 قاعدة عامة (نيون/توهج داخل القاعة)
- **لا تركّب أي نيون أو لوحة متوهّجة على مستوى خط نظر اللاعب أمام الشاشة.** النيون يكون عند **الأرضية أو السقف**، بعيداً عن جدار الشاشة، ودائماً راقب الـ`BloomEffect` (سبق أن سبّبت الألواح الزرقاء «شريطاً سماوياً» يغطّي اسم السينما فأُزيلت نهائياً).
- أي لوحة سطحية (SurfaceGui) تُثبَّت ملاصقة لواجهة المبنى (`Z ≈ −100.5`) ولا تُترك طايفة للأمام.

---

## 9) الواجهات البرمجية المشتركة (`_G`) — للربط بين الأنظمة

الأنظمة تتواصل عبر دوال عامة (مع فحص `nil` لتحمّل ترتيب التحميل):
- اقتصاد: `_G.GetCoins` · `_G.AddCoins` · `_G.GetTickets` · `_G.UseTickets` · `_G.AddTickets` · `_G.IsVIP`
- إشعارات/إدارة: `_G.NotifyPlayer` · `_G.ChatBroadcastAdmin` · `_G.AdminSetMarquee`
- كتم: `_G.ChatIsMuted` · `_G.ChatSetMuted`
- أوامر: `_G.ChatCmdConfigGet` (قائمة الأوامر ومستوياتها للوحة الإدارة) · `_G.ChatCmdConfigSet(key, level)` (تغيير مستوى أمر بعد التحقق من رتبة الطالب)

---

## 10) 🧗 نظام الباركور الجديد (كامل) — كيف تكمّله وتعدّله

> الباركور أُعيد بناؤه **من الصفر** بقطع روبلوكس أصلية فقط (Part/Cylinder) داخل `src/ParkourSystem.server.lua` — صفر Union/Mesh. **كل شيء في هذا الملف:** الهندسة + الميكانيكا + الضابط + البوّابة. تعديل أي شيء = عدّل هنا ثم `python3 build_all.py`.

### الفكرة بإيجاز
مسار خطّي صاعد بهدوء: **١٤ منصّة** (مرقّمة 0..13) على **٤ مناطق ملوّنة** بالتقدّم (أخضر ← فيروزي ← برتقالي ← أحمر). نقاط حفظ + حواجز حمراء قاتلة + عوائق دوّارة + منصّة قفز + سقوط = رجوع + وصول للقمة = مكافأة. طابع «أسطح المدينة»: كل منصّة = سطح مبنى خرساني بنوافذ مضيئة + سور بلون المنطقة + زينة.

### الثوابت الأساسية (أعلى الملف ~٥٩–١٠٢؛ ما عدا `REWARD` فقرب منطق النهاية)
| الثابت | القيمة | المعنى |
|--------|--------|--------|
| `ORIGIN_X, ORIGIN_Z` | `-175, 20` | إحداثيات بداية المسار (سماء الركن الشمالي الغربي) |
| `TOP0` | `90` | ارتفاع سطح أول منصّة |
| `RISE` | `3.6` | الصعود بين كل منصّة (قفزة سهلة) |
| `STEP_X` / `STEP_Z` | `14` / `4` | تباعد/انزياح المنصّات |
| `PW, PD, PT` | `11, 11, 1.2` | عرض/عمق/سمك المنصّة |
| `COUNT` | `14` | عدد المنصّات (0..COUNT-1) |
| `REWARD` | `250` | كوينز مكافأة القمة |
| `ENTRY_POS` | `V(-20, 1.3, 60)` | موضع بوّابة الدخول (أرضية، جانب الانطلاق الرئيسي) |
| `EXIT_POS` | `V(6, 5, 60)` | موضع الخروج الآمن |
| `SPAWN_POS` | `V(ORIGIN_X, platTop(0)+3.5, ORIGIN_Z)` | نقطة ظهور اللاعب فوق المنصّة 0 |
| `FALL_Y` | `platTop(0) - 12` | تحتها = سقوط ⇒ رجوع لآخر نقطة حفظ |

دوال الموضع: `platTop(i) = TOP0 + i*RISE` (ارتفاع سطح المنصّة i) و `platCenter(i)` (مركز المنصّة i).

### جداول العناصر (هنا تتحكّم بكل شيء — غيّر المفاتيح فقط)
```lua
local CP_AT     = { [0]="١", [4]="٢", [7]="٣", [10]="٤" }  -- نقاط حفظ مرقّمة (المنصّة → رقم النقطة)
local STRIP_AT  = { [5]=true, [9]=true, [11]=true }         -- بلاطات/أشرطة حمراء قاتلة على هذه المنصّات
local SPIN_AT   = { [8]=1.1, [12]=1.5 }                     -- عوائق دوّارة (المنصّة → سرعة rad/s)
local BOUNCE_AT = { [6]=true }                              -- منصّة قفز نطّاطة
```
حلقة البناء تمرّ على `0..COUNT-1`، تبني كل منصّة وتطبّق عليها ما يخصّها من هذه الجداول تلقائياً.

### كيف تضيف/تعدّل (وصفات جاهزة)
- **زيادة عدد المنصّات:** ارفع `COUNT` (مثلاً 16) — تُبنى الجديدة وتُربط تلقائياً، ثم وزّع عليها نقاط حفظ/حواجز عبر الجداول.
- **إضافة حاجز أحمر قاتل:** أضف رقم المنصّة لـ `STRIP_AT` (مثلاً `[7]=true`) — لمسه يرجّع اللاعب لآخر نقطة حفظ.
- **إضافة نقطة حفظ:** أضف `[رقم_المنصّة]="رقم_النقطة"` في `CP_AT` (الأرقام عربية للعرض).
- **إضافة عائق دوّار:** `SPIN_AT[رقم] = السرعة` (rad/s؛ الأكبر أصعب).
- **منصّة قفز:** `BOUNCE_AT[رقم] = true`.
- **تغيير ألوان المناطق:** عدّل دالة `zoneColor(i)` أو ألوان `GREEN/TEAL/ORANGE/RED` أعلى الملف.
- **تغيير الصعوبة:** قلّل `RISE`/`STEP_X` (أسهل) أو زِدها (أصعب)، أو قلّل عدد مفاتيح `STRIP_AT`.

### أدوات البناء (استعملها لأي قطعة جديدة — لا تكرّر كوداً)
- `mk(name, size, posOrCF, color, material, parent, canCollide)` → صندوق `Part` (Anchored دائماً). `posOrCF` يقبل `Vector3` أو `CFrame`.
- `mkCyl(name, height, dia, centerY, x, z, color, material, parent, canCollide)` → أسطوانة عمودية.
- `signGui(part, text, color, size)` → لافتة نصّية تُقرأ من الوجهين.

### الدوال المفتاحية للميكانيكا
- `startRun(player)` → يبدأ الجولة + ينقل اللاعب لـ `SPAWN_POS` + يشغّل حلقة التقدّم. **بوّابة الدخول تستدعيها بعد العدّ التنازلي.**
- `stopRun(player)` → ينهي الجولة وينقل لـ `EXIT_POS`.
- `respawnAtCheckpoint(player)` → يرجّع اللاعب لآخر نقطة حفظ (عند لمس حاجز أحمر أو السقوط تحت `FALL_Y`).
- `teleportTo(player, cf)` · `rlAllow(userId)` (حارس إغراق Remotes).

### 👮 ضابط الشرطة البحريني (عند بداية المسار، ~٣٧٢)
- يُبنى **بقطع أصلية فقط (~٤٥ قطعة)**، Model باسم `PoliceOfficer`، **Anchored بالكامل** (بلا تصادم/فيزياء = صفر استهلاك).
- يقف **جانب** نقطة الانطلاق على منصّة البداية ويواجه اللاعب القادم (لا يعترض المسار).
- الهندسة مترجمة من مجسّم بلندر معتمد: تُحوّل إحداثيات بلندر (X يمين، Y أمام/خلف، Z أعلى) إلى إطار الضابط المحلي عبر مصفوفة دوران (`base`)، فكل القطع توضع نسبةً لاتجاهه.
- الزي: بدلة كحلية + باكِت أزرار ذهبية + بريه عنّابي + شارة كاب + نجوم رتبة + **علم البحرين** على الكُمّ الأيمن + ملامح وجه + بوت أسود.
- **لتعديله:** غيّر موضعه/اتجاهه عبر متغيّر `base` في تلك الدالة.

### 🚪 بوّابة الدخول 3D + ProximityPrompt + العدّ التنازلي (~٥٨٨–٧٦٠)
استُبدل **البدء التلقائي باللمس** بـ**بدء متحكَّم** (يمنع الانطلاق بالغلط):
- **الهندسة (قطع أصلية):** قاعدة إطلاق دائرية + حلقة ضوء خضراء (قرص نيون) + ٣ أذرع ضوء دوّارة + قوس بعمودين خرسانيين + لافتة «برج الباركور / اضغط E للبدء» + عمود ضوء يطلع للسماء + فقاعة عدّ تنازلي (BillboardGui).
- **التشغيل:** `ProximityPrompt` اسمه `StartParkour` على `promptPart` — `KeyboardKeyCode=E`, `MaxActivationDistance=14`, `HoldDuration=0`.
- **عند الضغط (`gatePrompt.Triggered`):** يتحقّق أن اللاعب ليس داخل جولة + لا يوجد عدّ جارٍ (علم `gateCountingDown`) ⇒ يعطّل البرومبت ⇒ يعرض **٣·٢·١** (ثانية لكل رقم عبر `setGateNumber`) + إشعار ⇒ ينادي `startRun(player)` ⇒ يعيد تفعيل البرومبت.
- **دوران الأذرع:** حلقة `task.spawn` تدوّر الأذرع **فقط عند وجود لاعب ضمن ٨٠ ستد** من `ENTRY_POS` (تنام `task.wait(0.5)` عند الخمول = صفر استهلاك).
- **لتعديل العدّ:** غيّر قائمة `{ "٣", "٢", "١" }` داخل `gatePrompt.Triggered`. **لموضع البوّابة:** `ENTRY_POS`. **لمدى التفعيل:** `MaxActivationDistance`. **لمسافة تشغيل الدوران:** الرقم `80`.

### الأداء (لا تكسره)
كل القطع `Anchored` (صفر فيزياء). حلقة المنطق الوحيدة تعمل **فقط أثناء جولة نشطة** وتتوقف عند الخمول. **أي عنصر تضيفه:** ثبّت `Anchored=true` ولا تشغّل حلقات دائمة بلا شرط قرب لاعب.

---

## 11) 🗺️ وين وصلنا — ملخّص آخر جلسة (الباركور الجديد + الضابط + البوّابة)

العمل على فرع `devin/1781734279-presidential-palace` (**PR #252**)، كله قطع أصلية (صفر Union/Mesh).

| العنصر | ماذا تم | الحالة |
|--------|---------|--------|
| 🧗 **نظام باركور جديد كامل** | أُعيد بناؤه من الصفر: ١٤ منصّة، ٤ مناطق ملوّنة، نقاط حفظ، حواجز حمراء قاتلة (٥·٩·١١)، عوائق دوّارة (٨·١٢)، منصّة قفز (٦)، أسهم، سقوط=رجوع، قمة=مكافأة (٢٥٠ كوينز + لقب «بطل الباركور»). أُزيل موديل الـObby القديم وكل الإيموجي. | مكتمل |
| 🎨 **طابع «أسطح المدينة»** | كل منصّة = سطح مبنى خرساني بنوافذ مضيئة + سور بلون المنطقة + زينة (مكيّفات/خزّانات/هوائيات) + أفق نواطح. | مكتمل |
| 👮 **ضابط شرطة بحريني** | ~٤٥ قطعة أصلية عند البداية، Anchored، يواجه اللاعب، زي كحلي + بريه عنّابي + علم البحرين. | مكتمل |
| 🚪 **بوّابة دخول 3D** | بدل البدء التلقائي باللمس: ProximityPrompt «اضغط E» + عدّ تنازلي ٣·٢·١ ثم `startRun`. حلقة ضوء تدور فقط عند اقتراب لاعب. | مكتمل |

### التحقّق المطبّق (نفّذه قبل أي تسليم)
- `selene src/` → ٠ أخطاء/٠ تحذيرات.
- `luau-analyze src/ParkourSystem.server.lua` بعد الفلترة → نظيف.
- `python3 build_all.py` يبني `DonationCity_FINAL.rbxlx` بنجاح (XML سليم) — تتحدّث كتلة «PARKOUR SYSTEM (Server)».

### ملاحظة لينت مهمّة (مهارة المشروع للأكواد Roblox/Luau)
استعمل **selene** أولاً (واعٍ بـRoblox، يفلتر «Unknown global»)، ثم **luau-analyze** مع فلترة الضجيج:
```bash
selene src/ParkourSystem.server.lua
luau-analyze src/ParkourSystem.server.lua 2>&1 | grep -vE "Unknown (global|type)"
```
الأداتان مثبّتتان في `~/.local/bin/`.

---

## 12) دليل البدء السريع للمطوّر الجديد

1. **اقرأ** هذا الملف + `README.md` كاملين.
2. عدّل السكربت المطلوب في `donation-city/src/`.
3. ابنِ: `python3 build_all.py` ثم تحقّق من XML + CDATA + صفر باك-دور.
4. افحص الصياغة (إلزامي): `selene src/` ثم `luau-analyze src/ 2>&1 | grep -vE "Unknown (global|type)"`.
5. افتح `.rbxlx` في Studio وانشره على Place `10237943037`.
6. جرّب من **اللعبة المنشورة** للمزايا المعتمدة على DataStore/Robux.
7. رقم الإصدار يُحدّث في `src/LoadingScreen.client.lua` (متغير `MenuVersion`).
8. لمنتج/باقة جديدة: عدّل `PRODUCT_IDS` / `CONFIG.*GamePassId` / `STORE_ITEMS` في `src/CinemaServices.server.lua`.
9. **لأي تعديل على الباركور** (منصّات/حواجز/نقاط حفظ/الضابط/البوّابة): عدّل `src/ParkourSystem.server.lua` فقط — راجع القسم ١٠ لجداول العناصر وأدوات البناء.

**عند أي مهمة جديدة:** التزم بقواعد القسم ٢، اعمل بحِرفية، وسلّم ملفاً نظيفاً مع شرح كيفية التجربة — دون اختبار داخل Studio بنفسك.

10. **لأي تعديل على القصر** (الغرف/الأثاث/الأبواب/الصباغة): عدّل `src/PalaceSystem.server.lua` — راجع الأقسام ١٣→١٥.
11. **لأي تعديل على البصمة/سجلّ الدخول:** الخادم في `src/PalaceSystem.server.lua` والواجهة في `src/PalaceClient.client.lua` — راجع القسم ١٥.
12. **لأي تعديل على الحرّاس:** عدّل `src/GuardSystem.server.lua` — راجع القسم ١٦.

---

## 13) 🏛️ القصر الجمهوري لشهد — كيف بُني وكيف تعدّله

> **مصدر الكود:** `src/PalaceSystem.server.lua` (سكربت سيرفر، ~١٣١٧ سطر). **كل شي يُبنى وقت التشغيل بقطع روبلوكس أصلية** (Part / Cylinder / Wedge / Truss) — **لا Union ولا Mesh ولا CSG**. القصر يُحذف ويُعاد بناؤه كل تشغيل، فأي تعديل بالكود يظهر فوراً بعد `build_all.py` + نشر.

### أ) البنية العامة
- يُبنى نموذجان (Models) داخل `Workspace`:
  - **`PalaceInterior`** — الأرضية + الجدران الداخلية + السقف + الغرف الأربع المؤثَّثة + الأبواب.
  - **`PalaceExterior`** — القاعدة الحجرية + الجدران الخارجية + البورتيكو + القبّة الذهبية + النوافذ + الأعلام + لافتة الواجهة.
- المدخل الرئيسي على جهة **−X** (يواجه المدينة)، وفتحته بين `ENT_Z0=11` و`ENT_Z1=25`.

### ب) ثوابت الهندسة (إحداثيات عالمية) — أعلى الملف
| الثابت | القيمة | المعنى |
|--------|--------|--------|
| `FX0, FX1` | `96, 240` | حدود الأرضية على محور **X** (الطول ١٤٤) |
| `FZ0, FZ1` | `-36, 72` | حدود الأرضية على محور **Z** (العرض ١٠٨) |
| `FLOOR_Y` | `0.6` | سطح الأرضية (تجلس على العشب عند Y=0) |
| `CEIL_Y` | `11.6` | ارتفاع السقف الداخلي |
| `WT` | `1` | سُمك الجدار |
| `ENT_X` | `96` (=FX0) | موضع فتحة المدخل على X |
| `ENT_Z0, ENT_Z1` | `11, 25` | عرض فتحة المدخل (١٤) |
| `DOOR_W0, DOOR_W1` | `126, 142` | فتحة الجدار الفاصل بين البهو والغرف الخلفية |

> **مهم:** هذه الإحداثيات **يجب أن تطابق** ثوابت `GuardSystem.server.lua` (المدخل عند x=96 يواجه −X). لو حرّكت القصر، حرّك مواقع الحرّاس (`POSTS`) معه — راجع القسم ١٦.

### ج) لوحة الألوان/الخامات — أعلى الملف
| الثابت | RGB | الاستخدام |
|--------|-----|-----------|
| `MARBLE` | `(206,200,188)` | الأرضية الرخامية |
| `WALL_C` | `(228,224,214)` | الجدران الداخلية |
| `TRIM_GOLD` | `(214,175,92)` | الزخارف الذهبية + الشعار + اللافتات |
| `CEIL_C` | `(234,230,222)` | السقف |
| `WOOD` | `(86,56,34)` | الأثاث الخشبي الفاتح |
| `WOOD_D` | `(64,40,24)` | الأثاث الخشبي الغامق |

كل غرفة تضيف لونها المميّز محلياً: البهو **كحلي** `NAVY (28,42,78)`، الاستقبال **بنفسجي** `PURPLE (120,55,150)`، الضيوف **أخضر مخضرّ** `(26,77,71)`، المكتب **ذهبي/بُنّي** `GOLDC (158,117,41)`.

### د) أدوات البناء (helpers) — استعملها بدل تكرار الكود
| الدالة | التوقيع | الوظيفة |
|--------|---------|---------|
| `part` | `part(name, cf, size, color, material, parent?)` | تنشئ Part مثبّت (`Anchored`, `CanCollide` افتراضياً) |
| `box` | `box(name, x0,x1, y0,y1, z0,z1, color, material, parent?)` | كتلة بحدود عالمية (يحسب المركز/الحجم تلقائياً) |
| `wall` | `wall(x0,x1, z0,z1)` | جدار داخلي من الأرضية للسقف |
| `lintel` | `lintel(x0,x1, z0,z1)` | عتبة علوية فوق فتحة باب |
| `pointLight` | `pointLight(parent, color, brightness, range)` | إضاءة موضعية دافئة |
| `goldStrip` | شريط ذهبي زخرفي على الجدار/الكورنيش |
| `rug` | `rug(x0,x1, z0,z1, color)` | سجّادة أرضية |
| `chandelier` | `chandelier(x, z, color)` | ثُريّا متدلّية مضيئة |
| `sofa` / `armchair` | `(cx, cz, rotY, color)` | كنبة / كرسي وثير (rotY بالدرجات) |
| `bookshelf` | `(cx, cz, rotY)` | مكتبة كتب |
| `painting` | `(cf, w, h, img?)` | لوحة جدارية (img اختياري) |
| `flag` / `drapes` | علم على سارية / ستائر |
| `bigDesk`/`deskChair`/`laptop`/`visitorChair`/`coffeeTable`/`plant`/`sconce` | عناصر المكتب الرئاسي |

### هـ) الغرف الأربع (الأثاث الفعلي لكل غرفة)
كل غرفة دالة بناء مستقلة (`do … end`) داخل الملف:

1. **البهو الكبير (Lobby)** — رخامي ذهبي/كحلي. سجّادة كحلية `rug(110-162, 6-30)`، ثُريّتان `(124,18)` و`(150,18)`، **مكتب استقبال** `ReceptionDesk` عند `(150,18)`، علمان على `(168,6)` و`(168,30)`.
2. **قاعة الاستقبال (Reception)** — بنفسجي. سجّادة، ثُريّتان، **٣ كنبات + كرسيّان وثيران**، لوحتان جداريتان، ستائر على الجدار `X=71.4`.
3. **غرفة الضيوف (Guests)** — أخضر مخضرّ هادئ. سجّادة، ثُريّتان، **٣ كراسي وثيرة + كنبة**، لوحتان.
4. **المكتب الرئاسي (Office)** — ذهبي/بُنّي فخم. ٤ ثُريّات، **مكتب تنفيذي** `bigDesk(226,22)` + كرسي جلد + لابتوب + كرة أرضية، **شعار رئاسي** على الجدار الخلفي، **مكتبتا كتب**، **صفّ ٣ كراسي زوّار** يواجه المكتب (طلب المالك)، ركنا جلوس جانبيان (كراسي + طاولات قهوة)، نباتات، إضاءات جدارية.

### و) كيف تعدّل القصر (وصفات جاهزة)
- **تغيير لون غرفة:** عدّل ثابت اللون المحلي في دالة الغرفة (مثل `NAVY` في البهو).
- **إضافة أثاث:** نادِ الـhelper المناسب داخل دالة الغرفة، مثلاً:
  ```lua
  sofa(130, 64, 180, SOFA_C)        -- كنبة جديدة في الاستقبال (x=130, z=64, دوران 180°)
  chandelier(140, 18, warm)         -- ثُريّا إضافية في البهو
  ```
  احرص أن تكون الإحداثيات داخل حدود الغرفة وألا تتعارض مع الأبواب.
- **تكبير/تصغير القصر:** عدّل `FX0/FX1/FZ0/FZ1` ثم **حرّك مواضع الأثاث والأبواب والحرّاس بنفس الإزاحة**.
- **تغيير ارتفاع السقف:** عدّل `CEIL_Y`.
- **لافتة الواجهة الخارجية** «مكتب القصر الجمهوري لشهد» نصّها في دالة بناء `PalaceExterior` (متغيّر `fl.Text`). لتغيير الاسم عدّله هناك + في لافتة المدخل الداخلية (`label.Text`).

---

## 14) 🚪 أبواب الغرف (E) + لافتات الأسماء

- كل غرفة لها **باب مزدوج** يُفتح/يُغلق بالضغط على **E** عبر `ProximityPrompt`.
- الدالة `makeDoubleDoor(prefix, axis, wallPos, a, b, leftDeg, rightDeg)` تبني مصراعَين وتُرجع دالة `setState(toOpen, duration)` تحرّك المصراعين بـ`TweenService`.
- الدالة `attachPrompt(setState, anchorCF, label)` تركّب فقاعة «ادخل …» وتربط الفتح/الإغلاق.
- الأبواب الثلاثة وعناوينها:
  | الباب | الموضع | اللافتة |
  |-------|--------|---------|
  | `recDoor` | منتصف فتحة الجدار، `z=34.5` | «قاعة الاستقبال» |
  | `gstDoor` | منتصف فتحة الجدار، `z=1.5` | «غرفة الضيوف» |
  | `offDoor` | `x=176.5`، منتصف المدخل | «المكتب الرئاسي» |
- فوق كل باب **لافتة ذهبية متوهّجة** باسم الغرفة (دالة لافتة مشتركة، `Material.Neon` + `SurfaceGui`).

**لإضافة باب جديد:** نادِ `makeDoubleDoor(...)` لبناء المصراعين، ثم `attachPrompt(setState, anchorCF, "اسم الغرفة")`. **لتغيير اسم غرفة:** عدّل نصّ الـ`label` في استدعاء `attachPrompt` المقابل + اللافتة الذهبية فوقه.

---

## 15) 🔐 قفل البصمة + سجلّ الدخول

> الخادم: `src/PalaceSystem.server.lua` (الجزء السفلي). الواجهة: `src/PalaceClient.client.lua`.

### أ) الثوابت (أعلى السكربت)
| الثابت | القيمة | المعنى |
|--------|--------|--------|
| `SCAN_TIME` | `2.3` | مدة «المسح» قبل ظهور النتيجة (ثوانٍ) |
| `DOOR_OPEN_T` | `2.6` | مدة فتح باب المدخل تدريجياً |
| `DOOR_HOLD` | `5.0` | يبقى المدخل مفتوحاً قبل أن يُقفل تلقائياً |

### ب) التحقّق من الصلاحية — `isAdmin(player)`
```lua
-- يستعمل واجهة الدردشة المشتركة _G.GetChatRank
if type(_G.GetChatRank) == "function" then
    local ok, rank = pcall(_G.GetChatRank, player)
    if ok and (rank == "owner" or rank == "admin") then return true end
end
return false
```
> **لتغيير من يُسمح له بالدخول:** التحكّم مركزي عبر الرتب (owner/admin). لا تضع UserIds مكرّرة هنا — أضِف الشخص لرتبة admin في نظام الرتب وسيعمل في كل مكان (راجع القسم ٩ — الواجهات المشتركة).

### ج) تسلسل المسح (RemoteEvents داخل مجلد `PalaceNet` في `ReplicatedStorage`)
1. اللاعب يضغط **E** على جهاز البصمة → السيرفر يُطلق `StartScan` للعميل.
2. العميل يعرض **شاشة المسح** (`PalaceScanGui`): حلقات بصمة مرسومة بقطع أصلية + خطّ مسح متحرّك + أصوات (نبضة/نجاح/رفض).
3. بعد `SCAN_TIME`: إن كان `isAdmin` → `ScanResult(true)` (اللوحة تخضرّ) + `recordEntry(player)` + `openDoorsSequence()` (يفتح المدخل ثم يُقفل بعد `DOOR_HOLD`). وإلا → `ScanResult(false)` (اللوحة تحمرّ، رفض).

### د) سجلّ الدخول (DataStore)
- يُخزَّن في DataStore باسم **`PalaceAccessLog_v1`**، المفتاح **`"log"`**.
- `recordEntry(player)` يُدرج `{ name = player.Name, t = os.time() }` في مقدّمة الجدول، مع **سقف** (يُزيل الأقدم بـ`table.remove`).
- العميل يطلب السجلّ بـ`LogRequest`؛ السيرفر يردّ بـ`LogData`: **للأدمن** يرسل الجدول كاملاً، **لغيره** يرسل `false`. الواجهة `PalaceLogGui` تعرضه للأدمن فقط.

### هـ) كيف تعدّل
- **مدة المسح:** غيّر `SCAN_TIME`. **توقيت الباب:** `DOOR_OPEN_T` / `DOOR_HOLD`.
- **مظهر شاشة المسح/أصواتها:** في `PalaceClient.client.lua` (ألوان/شفافية/معرّفات الأصوات).
- **تصفير السجلّ:** غيّر اسم الـDataStore إلى `..._v2` (إصدار جديد فارغ) بدل المسح اليدوي.

---

## 16) 👮 نظام الحرّاس (Guards)

> المصدر: `src/GuardSystem.server.lua` (~٣٧٧ سطر). **مستقل تماماً عن ضابط الباركور** (ذاك ٤٥ قطعة ثابتة بلا حركة؛ هؤلاء R6 أحياء بأنميشن + Pathfinding).

### أ) كيف يُبنى الحارس
- يُبنى عبر **`HumanoidDescription`** (R6 كامل) + سكربت **Animate** قياسي، فيأخذ ريﭺ ومفاصل طبيعية.
- الزيّ: `Shirt`/`Pants` مباشرة (لا عبر كتالوج):
  | الثابت | القيمة |
  |--------|--------|
  | `SHIRT_ID` | `1588379400` |
  | `PANTS_ID` | `1588492725` |
- **قبّعة الفرو `GuardCap`** مبنية بقطع أصلية وملحومة على الرأس (مخزّنة بإحداثيات نسبية للرأس).
- الأنميشن: `IDLE_ID = "rbxassetid://180435571"`, `WALK_ID = "rbxassetid://180426354"`, `WalkSpeed = 9`.

### ب) المواقع (`POSTS`) — تطابق غرف القصر
| الاسم | الإحداثي `ground(x,z)` | الاتجاه | الدور |
|-------|------------------------|---------|-------|
| حارس المدخل | `(93, 12)` | غرب (−X) | sentry (ثابت) |
| حارس المدخل | `(93, 24)` | غرب | sentry |
| حارس البهو | `(118, 11)` | غرب | **lead** (يرافق) |
| حارس البهو | `(118, 25)` | غرب | **follow** (يتبع خلف الزائر) |

- `ground(x,z)` ترفع المركز إلى `FLOOR_Y(0.6) + R6_HIP(3.0)` فوق الأرضية.
- `LOBBY_TARGET = ground(152, 18)` — وسط البهو الذي يقودك إليه الحارس القائد.
- `ENTRY_MIN/ENTRY_MAX` — صندوق كشف اقتراب الزائر من المدخل.

### ج) السلوك
- `GREET_RADIUS = 18`، `GREET_COOLDOWN = 12` ثانية.
- عبارات الترحيب `PHRASES` (٥ جُمل عربية مثل «أهلاً بك في القصر الجمهوري»، «تشرّفنا بحضورك») تظهر كفقاعة فوق الحارس عبر `speak()`.
- عند اقتراب لاعب: الحارس **القائد (lead)** يقول «تفضّل من هنا، سأرافقك» ويمشي إلى `LOBBY_TARGET` عبر `PathfindingService`، والحارس **التابع (follow)** يسير خلف اللاعب؛ وكلاهما يرجع لموقعه (`homeCF`) عند خروج اللاعب.

### د) كيف تعدّل
- **إضافة حارس:** أضِف صفّاً جديداً لجدول `POSTS` بالإحداثي والاتجاه والدور.
- **تغيير عبارات الترحيب:** عدّل جدول `PHRASES`.
- **مدى/تبريد الترحيب:** `GREET_RADIUS` / `GREET_COOLDOWN`. **سرعة المشي:** `WalkSpeed`.
- **الزيّ:** `SHIRT_ID` / `PANTS_ID`. **الأنميشن:** `IDLE_ID` / `WALK_ID`.
- **مهم:** لو حرّكت القصر (القسم ١٣)، حدّث `POSTS` و`LOBBY_TARGET` و`ENTRY_MIN/MAX` بنفس الإزاحة وإلا وقف الحرّاس في غير أماكنهم.

---

## 17) 🔧 خط الحقن (Injection Pipeline) — كيف تدخل الأنظمة لملف اللعبة

> ملف اللعبة `DonationCity_FINAL.rbxlx` يُبنى ببايثون. فيه نوعان من السكربتات:

### أ) `build_all.py` — البناء الروتيني (سكربتات `src/`)
- يبحث عن **علامات فريدة** داخل كتل `CDATA` في الـ`.rbxlx` ويستبدل محتوى السكربت بأحدث نسخة من `src/`.
- يشمل من هذه الجلسة كتل: **PARKOUR SYSTEM (Server)**، **PALACE SYSTEM (Server)**، **PALACE CLIENT (Client)**، **GUARD SYSTEM (Server)**.
- **هذا هو الأمر الذي تشغّله بعد أي تعديل على `src/`:** `python3 build_all.py`.

### ب) سكربتات الحقن لمرّة واحدة (idempotent) — أنشأت البنية الثابتة
| السكربت | ماذا يفعل |
|---------|-----------|
| `remove_palace_csg.py` | يحذف موديلات CSG القديمة للقصر/جهاز البصمة (placeholders) ويقلّم كتل `SharedString` غير المرجعيّة (القشرة والداخل الآن قطع أصلية وقت التشغيل). |
| `inject_palace.py` | يزيل أصول الشاطئ، يدمج موديل القصر النظيف ثم **ينحت تجويفاً داخلياً** (`CARVE_INTERIOR`/`CARVE_CORRIDOR`) لإفراغ المكان للغرف المبنية بالكود. |
| `inject_palace_systems.py` | يضيف/يحدّث سكربت **PalaceSystem** (ServerScriptService) + **PalaceClient** (StarterPlayerScripts) + **يركّب جهاز البصمة** النظيف عند الإحداثي المحدّد. |
| `inject_guards.py` | يحذف قالب `GuardCap` القديم من ServerStorage، يضيف/يحدّث سكربت **GuardSystem**، ويصلح مراجع `SharedString` المعلّقة حتى يفتح Studio بلا خطأ. |
| `remove_parkour_course.py` | يحذف موديل الـObby المستورد القديم (استُبدل بالباركور المبني بالكود). |

- **مصادر الموديلات النظيفة** في `donation-city/assets/` (مثل `palace1.rbxmx`، `fingerprint.rbxmx`، `guard.rbxmx`). كلها فُحصت من الباك-دور قبل الدمج (القاعدة ٣ في القسم ٢).
- **متى تشغّلها؟** نادراً — فقط لو احتجت إعادة بناء البنية الثابتة من الصفر. للتعديل اليومي على المنطق/الغرف يكفي تعديل `src/` ثم `build_all.py`.

---

## 18) ✉️ رسالة ترحيب للمطوّر الجديد (انسخها وأرسلها كما هي)

> هذا نصّ جاهز ترسله **لأي مبرمج جديد** (أو تستعمله كأول رسالة لجلسة Devin جديدة) مع رابط الملف:

```text
أهلاً، أنت تستلم مشروع لعبة «مدينة التبرعات» على Roblox.

قبل أي شغل، اقرأ ملف التسليم كاملاً:
donation-city/docs/HANDOFF_للمطور_الجديد.md

أهم القواعد (لا تتجاوزها):
1) عدّل في donation-city/src/ فقط — لا تعدّل داخل ملف الـ.rbxlx يدوياً.
2) بعد التعديل ابنِ الملف:  python3 build_all.py
3) افحص الصياغة (إلزامي قبل التسليم):
   selene src/
   luau-analyze src/ 2>&1 | grep -vE "Unknown (global|type)"
4) كل شي يُبنى بقطع روبلوكس أصلية فقط — صفر Union/Mesh/CSG.
5) أي موديل خام (rbxmx/rbxm) افحصه من الباك-دور قبل ما تدخله (راجع القسم ٢).
6) لا تختبر داخل Studio بنفسك — صاحب المشروع يختبر بعد التسليم؛ سلّم ملفاً نظيفاً + شرح كيف يجرّب.
7) ⭐ اقرأ القسم ٢٠ (قواعد/سياسات جديدة إلزامية): لا تحذف تحديثات قديمة من سجلّ اللعبة، انشر فوراً عبر publish.py، الصورة المرجعية بالعربي raqm/rtl، الجوال-أولاً.

من أين تبدأ القراءة حسب المهمة:
- ⭐ ملخّص آخر جلسة كاملاً: القسم ١٩. والقواعد الجديدة: القسم ٢٠.
- إعداد عام/البنية/القواعد: الأقسام ١ و٢ و٣.
- البناء والنشر التلقائي (publish.py) واللينت: الأقسام ٤ و١٢.
- الطيران /fly (خصوصاً الجوال): القسم ١٩-هـ، وعدّل src/CustomChat.client.lua.
- حاجز حدود الماب + شبكة الأمان: القسم ١٩-و، وعدّل src/BoundarySystem.server.lua.
- صندوق المكافآت / إكس-أو / تحكّم وقت اليوم: القسم ١٩ (ب/د).
- الباركور (المنصّات/الحواجز/نقاط الحفظ/الضابط/البوّابة): القسمان ١٠ و١١، وعدّل src/ParkourSystem.server.lua.
- القصر الجمهوري لشهد (الغرف/الأثاث/الصباغة/الأبواب): الأقسام ١٣ و١٤، وعدّل src/PalaceSystem.server.lua.
- قفل البصمة + سجلّ الدخول: القسم ١٥ (سيرفر src/PalaceSystem.server.lua + واجهة src/PalaceClient.client.lua).
- الحرّاس (الترحيب/المرافقة/المواقع): القسم ١٦، وعدّل src/GuardSystem.server.lua.
- إدخال أنظمة/موديلات جديدة لملف اللعبة: القسم ١٧ (build_all.py + سكربتات inject_*.py).

ملاحظة مهمة: إحداثيات القصر (القسم ١٣) ومواقع الحرّاس (القسم ١٦) مرتبطة —
لو حرّكت القصر، حرّك الحرّاس بنفس الإزاحة.

المخزن: github.com/alitravians/Ali — كل شي داخل مجلد donation-city/.
أي إضافة مهمة لازم تنزل على المخزن عبر فرع + PR (سكربتات src/ + ملف DonationCity_FINAL.rbxlx المبني).
```

---

## 19) 🆕 آخر جلسة — ملخّص كل ما أُنجز (اقرأه أولاً)

> جلسة كبيرة فيها أنظمة جديدة + إصلاحات جذرية + أتمتة النشر. كلها قطع روبلوكس أصلية (صفر Union/Mesh/CSG) ونُشرت على روبلوكس حتى النسخة الداخلية **v389**. المصدر الحقيقي دائماً في `src/`، والبناء عبر `build_all.py`، والنشر عبر `publish.py` (القسم ٤).

### أ) أتمتة النشر — `publish.py` + `PUBLISH_GUIDE.md`  (PR #262 · #263)
- سكربت ينشر مباشرة على روبلوكس عبر **Open Cloud API** بدون فتح Studio (فحص → بناء → رفع → طباعة رقم النسخة). يقرأ `ROBLOX_API_KEY`. **هذا هو سير العمل المعتمد الآن** — راجع القسم ٤.

### ب) أنظمة لعب جديدة
| النظام | الملفات (`src/`) | الوصف | PR |
|--------|------------------|-------|----|
| 🎁 **صندوق المكافآت** | `RewardBoxSystem.server.lua` · `RewardBoxClient.client.lua` (+ `inject_reward_box.py`) | صندوق تفاعلي: ٤ مهام تُكمَل → ١٠٠٠ كوينز + إنجاز «داعم مدينة شهد». | #261 |
| ⭕ **إكس-أو ثلاثية الأبعاد** | `TicTacToe.server.lua` (+ `inject_tictactoe.py`) | ٣ طاولات ملكية + كراسي + لعب بالتناوب. أُزيح صفّ الطاولات لليمين (0/26/52 على z=70) ليبتعد عن منارة الباركور (كان الكرسي يتداخل معها). + إصلاح: منع لاعب ثالث من طرد لاعب من كرسيّه أثناء المباراة. | #269 |
| 🪑 **كراسي البوث 3D** | `BoothSystem.server.lua` | كراسي بقطع روبلوكس أصلية. | #253 |

### ج) الـSpawn السينمائي (الريسبون الملكي) — `SpawnCinematic.server.lua` · `SpawnCinematic.client.lua`  (PR #258 · #265)
- ريسبون على طريقة ببجي: **طائرة شهد** عالية + إضاءة LED + برشوت، جوال-أولاً. مقصورة الطائرة **قابلة للمشي**؛ فتح الباب والقفز بالـ**E** (واقعي). + ضبط وضعية الهبوط بالمظلّة لتركّب صح على اللاعب.
- **تسريع النزول من الطائرة:** استمر بالضغط على **Shift** (كمبيوتر) أو زر **«تسريع ⬇»** (جوال) لزيادة سرعة الهبوط — يشتغل في طوري السقوط الحر والمظلّة، وعدّاد السرعة بالـHUD يعكسه لحظياً.
- **تحذير:** هذا النظام يعطّل تحكّم اللاعب مؤقتاً أثناء السينماتيك — أي نظام آخر يلمس تحكّم اللاعب يجب ألا يتعارض معه (راجع إصلاح الطيران في «و»).

### د) نظام الليل/النهار + ساعة السيرفر + القائمة الملكية — `DayNightCycle.server.lua` · `DayNightClient.client.lua` · `LoadingScreen.client.lua`
- **نظام تعاقب الليل والنهار (٥ مراحل)** بتوقيت البحرين الحقيقي يحسب الإضاءة كل ثانية لكل اللاعبين.
- **ساعة السيرفر** أعلى الشاشة تتحدّث لحظياً لكل اللاعبين.
- **قائمة رئيسية ملكية جديدة** (بطاقات فاخرة + شعار تاج + أيقونات) + سجلّ تحديثات بخطّ زمني ووسوم تصنيف ملوّنة.

### هـ) تحكّم الأدمن بوقت اليوم — `DayNightCycle.server.lua` · `CinemaServices.server.lua` · `CinemaClient.client.lua`  (PR #270 · #272)
- قسم جديد في لوحة الإدارة ← تبويب **«🎬 العرض»** (للأدمن فأعلى فقط): أزرار **☀️ صباح · 🌞 ظهر · 🌇 مغرب · 🌙 ليل** + **🔄 تلقائي** (يرجع لتوقيت البحرين الحقيقي).
- آلية: أضفنا **وضع override** يثبّت مرحلة مختارة فوق حساب الوقت التلقائي. يُطبَّق فوراً + يُحفظ في **DataStore** + يتزامن عبر السيرفرات بـ**MessagingService** (نفس أسلوب وضع الصيانة).
- إصلاحات لاحقة: غروب ذهبي دافئ لوضع «المغرب» + ضبط حساب بداية اليوم + تصغير الأزرار لتتناسق مع الهاتف.

### و) ⭐ إصلاح طيران الجوال الجذري — `CustomChat.client.lua`  (PR #271 · #273)
> **هذا أهم إصلاح في الجلسة — افهم السبب الجذري جيداً قبل لمس كود `/fly`.**
- **السبب الجذري:** `/fly` يفعّل `Humanoid.PlatformStand = true` (يفصل تحكّم الجسم عشان يطير). **روبلوكس يعطّل عصا التحكّم الافتراضية تلقائياً** في هذي الحالة، فعلى الجوال كان اللاعب يصير **ثابتاً بلا حركة أفقية** (الكمبيوتر يشتغل لأن WASD يُقرأ مباشرة).
- **الحل:** عصا تحكّم **خاصّة بالطيران** على يسار الشاشة، مستقلّة تماماً عن عصا روبلوكس — تقرأ اللمس مباشرة من `UserInputService` (متجه `joyVec ∈ [-1,1]²`). مع أزرار ⬆/⬇ و⚡. هرمية الإدخال: WASD (كمبيوتر) أولاً ثم `joyVec` (جوال).
- **نقاط حسّاسة (لا تكسرها):**
  - إطارا العصا (`joyBase` + `knob`) فيهما `Active = true` لِيَمتصّا اللمس فلا تدور الكاميرا.
  - اتصالات العصا **دائمة** (تُبنى مرّة واحدة في `buildFlyPad`) ومحميّة بعلم `flying` — **لا** تُضاف إلى `flyConns` (التي تُفصل في `stopFly`)، وإلا تنكسر العصا بعد أول إيقاف.
  - `setDefaultControls(false/true)` (إخفاء/إرجاع عصا روبلوكس) **محميّة بـ `UserInputService.TouchEnabled`** = جوال فقط، فلا نلمس تحكّم الكمبيوتر إطلاقاً (حتى لا نتعارض مع سينماتيك الـspawn في «ج»). والإخفاء يُستدعى **آخر `startFly`** بعد تجهيز BodyVelocity/Gyro فلا توجد لحظة تجمّد.

### ز) حاجز حدود الماب + شبكة أمان السقوط — `BoundarySystem.server.lua`  (PR #269)
- **المشكلة:** اللاعب يوصل حافة الماب ويرمي نفسه ويموت = تجربة سيّئة.
- **الحل (طبقتان، بلا سور مرئي — صاحب المشروع رفض السور الزجاجي):**
  1. **حاجز خفيّ صلب** على كامل محيط الماب — **يُقاس تلقائياً وقت التشغيل** على أبعد نقطة أرض فعلية (مو رقم ثابت!) فيلتفّ على آخر حافة مهما كان شكل الماب. (الدرس: افتراض «الماب مربّع ±٢٠٠» كان خطأ — الأرض غير متناظرة.)
  2. **شبكة أمان للسقوط:** لو نزل تحت حدّ Y لأي سبب → يرجع للسبون بنعومة **بلا شاشة موت**.
- للتشخيص البصري وقت التطوير: يمكن إظهار الجدران بلون أحمر شفّاف مؤقتاً ثم إخفاؤها.

### ح) إعادة تصميم الباركور + لافتة «أفضل وقت» (HoloBoard) — `ParkourSystem.server.lua` · `ParkourClient.client.lua`  (PR #254–#257 · #260)
- إعادة تصميم: ٥ مراحل متدرّجة + ٢٨ منصّة + ساحة معزولة + ضابط بوّابة بالزيّ التكتيكي + بوّابة رخامية + مسح ضوئي 3D بصوت عند الدخول + إصلاح تصادم الكرة البندولية.
- **لافتة «أفضل وقت»:** أُعيد تصميمها (شريحة أوسع، نص متمركز بلا طفح، خط فاصل ذهبي). إصلاحان: العمود الأوسط كان يغطّي النص → عمودان جانبيان؛ واللافتة كانت `CanCollide=false` (تُخترَق) → صارت صلبة.

### ط) جهاز البصمة المزدوج + إعادة التصميم — `PalaceSystem.server.lua` · `PalaceClient.client.lua`  (PR #264 · #268)
- **بصمتان:** تذكرة دخول مدفوعة (يُخصم ٢٥٠ كوينز عند كل محاولة دخول + منع الدخول المجاني خلف الدافع) + جهاز خروج داخلي (أوبسيديان زمردي) لا يُضغط من خارج القصر.
- **إعادة تصميم جهاز البصمة:** لوحة جدارية بيومترية فاخرة + شاشة مسح ثلاثية الأبعاد واقعية (التفاصيل الكاملة في القسم ١٥).

### ي) إصلاح السينما — `CinemaSystem.server.lua` · `CinemaServices.server.lua`  (PR #259)
- إصلاح شامل للبروجكتر: إضاءة السقف + أسماء + إزالة كود ميت + `WeldConstraint`.

---

## 20) ⚠ قواعد/سياسات جديدة إلزامية (ثبّتها صاحب المشروع هذه الجلسة)

> إضافة على قواعد القسم ٢ — هذه فرضها المالك صراحةً عبر هذه الجلسة، التزم بها حرفياً.

1. **سجلّ التحديثات داخل اللعبة — لا تحذف أبداً.** قائمة «تحديثات اللعبة» في `LoadingScreen.client.lua` (`CONFIG.UpdatesLines`). **أضِف الجديد فوق فقط** واحتفظ بكل التاريخ — **ممنوع** حذف إصلاحات قديمة لإضافة جديدة. (المالك انزعج من حذف إدخالات سابقة.) ومع كل نشر **بمّ** `CONFIG.MenuVersion` / نصّ «الإصدار»، وأضِف **سطر عربي** يلخّص التغيير. ملاحظة: «رقم الإصدار» المعروض للاعبين رقم تسلسلي عربي مستقل عن رقم نسخة روبلوكس (vNNN من `publish.py`).

2. **انشر فوراً بدون استئذان.** بعد أي إصلاح/ميزة: أصلِح → افحص → ابنِ → **انشر عبر `publish.py --skip-lint`** فوراً (لا تنتظر إذن المستخدم للنشر) → ثم بلّغه بالنسخة واطلب التجربة. كل عمل ينزل على فرع + PR.

3. **الصورة المرجعية بالعربي — السياسة الصحيحة (مهمة جداً، المالك تعب من تكرار الخلل):** عند رسم أي صورة مرجعية فيها نصّ عربي استعمل **Pillow مع raqm** و`direction="rtl"` و**خط `Noto Naskh Arabic`** — **بدون** إعادة تشكيل (reshape) أو bidi يدوي (raqm يسوّيه تلقائياً؛ المعالجة المزدوجة تطلع النص معكوس/مكسور). تجنّب الرموز اللي تطلع مربّعات. **تأكد من الصورة بصرياً قبل إرسالها** للمستخدم بدون ما يذكّرك. (أدوات الرسم في `/home/ubuntu/tools/`.)

4. **الجوال-أولاً — اختبر كل طرق الإدخال.** أي ميزة حركة/تحكّم يجب أن تعمل على **الجوال والكمبيوتر**. تذكّر: `PlatformStand=true` يعطّل عصا روبلوكس الافتراضية، فأي تحكّم جوال أثناءه يحتاج **عصا/أزرار لمسية مخصّصة** عبر `UserInputService` (راجع القسم ١٩-و). وأي تعطيل/تفعيل لتحكّم روبلوكس الافتراضي **احرسه بـ `UserInputService.TouchEnabled`** حتى لا تلمس تحكّم الكمبيوتر وتتعارض مع أنظمة أخرى (سينماتيك الـspawn).

5. **ميزات الأدمن في لوحة الإدارة فقط وبتناسق تام.** أي أداة إدارية جديدة (مثل تحكّم وقت اليوم) تُوضع داخل لوحة الإدارة، **ظاهرة للأدمن فأعلى فقط**، وبأزرار **متناسقة** مع بقية اللوحة و**متجاوبة مع الهاتف** (أحجام/خطوط لا تطفح). المالك اعتبر تناسق الأزرار «أهم خطوة».

6. **الحدود/الأبعاد تُقاس وقت التشغيل، لا تُثبَّت برقم.** أي حاجز/حد يعتمد على شكل الماب يجب أن **يُقاس تلقائياً** على الأرض الفعلية (راجع `BoundarySystem`)، لأن الماب غير متناظر؛ الأرقام الثابتة تصدّ اللاعب في النص.

7. **الإصلاح جذري وكامل دائماً** (تأكيد لقاعدة القسم ٢-٥): عالِج السبب الجذري + طبقات حماية + احتياطات (شبكة أمان، fallback). اللاعب يجب ألا يرى حالة معطوبة/تجمّد أبداً.
