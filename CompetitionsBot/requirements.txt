discord.py==2.4.0
aiohttp==3.10.10
python-dotenv==1.0.1
aiosqlite==0.20.0

# Sentry error monitoring. Init is fully gated behind SENTRY_DSN env var
# (see bot/observability.py) — leave the secret unset to fully disable.
sentry-sdk[aiohttp]>=2.18,<3
